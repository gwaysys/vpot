# 构建产物(由 build-msi.sh 生成,勿提交)
*.exe
*.msi
install-image.cmd
docker-compose.yaml
readme.txt
docker/wix311-binaries.zip
docker/wine-mono-x86.msi

# WiX 中间产物(容器内 /tmp 编译,勿提交)
*.wixobj
*.wixpdb
product.gen.wxs

# MSI 表导出(msiinfo dump 副产物,勿提交)
*.idt

# 构建上下文注入产物(勿提交)
docker/go-msi
docker/templates

# 构建复制产物(picoclaw-docker 来源,勿提交)
image-guide.png

# go-msi 构建中间产物
docker/go-msi
docker/templates
Binary/
